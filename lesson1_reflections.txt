How did viewing a diff between two versions of a file help you see the bug that was inroduced? It did.

How could having easy acces to the entire history of a file make you a more efficient programmer in the long term? It would make things easier.

What do you think are the pros and cons of manually choosing when to create a commit, like you do in Git, vs having versions automatically saved, like Google docs does? Con-might miss a commit. Pro-can strategically commit.

Why do you think some version control systems, like Git, allow saving multiple files in one commit, while others, like Google Docs, treat each file separately? different applications.
